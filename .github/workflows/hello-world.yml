name: hello-world

on:
  push:
    branches:
      - main

jobs:
    build:
        runs-on: ubuntu-latest        
        steps:
            - name: Checkout repository
              uses: RCFixer/hexlet-my-first-workflow
            - name: Run make say-hello
              run: make say-hello
